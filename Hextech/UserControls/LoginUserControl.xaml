<UserControl x:Class="Hextech.UserControls.LoginUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hextech.UserControls"
             xmlns:vm="clr-namespace:Hextech.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:LoginUserControlViewModel x:Name="vm"/>
    </UserControl.DataContext>

    <Grid>
        <Grid VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="6"/>
                <RowDefinition Height="32"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="{Binding Status}" TextAlignment="Center" FontSize="22"/>
            
            <Button Content="Login" Grid.Row="2" Width="100" Click="Button_Click" Visibility="{Binding LoginButtonVisibility}"/>
        </Grid>
    </Grid>
</UserControl>
